import { birthday } from "./birthday";
import { role } from "./role";
import { channel } from "./channel";
import { inviteRole } from "./inviteRole";

export const subcommands = [ role, birthday, channel, inviteRole ];